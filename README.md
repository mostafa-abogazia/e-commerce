
  # Code summary request

  This is a code bundle for Code summary request. The original project is available at https://www.figma.com/design/PLohEmSYDdaHFN3pNtowut/Code-summary-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  